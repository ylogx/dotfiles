language: python
services:
  - docker
cache: pip
python:
  - 3.6
before_install:
  - pip install -U pip
install:
  - pip install ansible-lint
script:
  - ./scripts/lint.sh
after_success:
  - travis_wait 30 make build  # Docker container build
  - docker run dotfiles /bin/sh -c 'ls -la ~'

  #- piprot requirements-dev.txt --outdated || true
  #- python -m flake8 || true
  #- make coverage
