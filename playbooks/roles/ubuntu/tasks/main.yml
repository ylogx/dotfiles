---
# Packages are now managed by chezmoi run_once scripts.
# See home/.chezmoiscripts/run_once_before_install-packages.sh.tmpl

- name: Change shell to ZSH
  become: true
  become_method: sudo
  user:
    name: "{{ ansible_user }}"
    shell: /bin/zsh
