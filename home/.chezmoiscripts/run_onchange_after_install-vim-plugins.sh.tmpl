#!/bin/bash
# dot_vimrc hash: {{ include "dot_vimrc" | sha256sum }}
# Re-runs automatically when .vimrc changes
if command -v vim &>/dev/null && [ -f ~/.vim/autoload/plug.vim ]; then
    echo "Installing/updating Vim plugins..."
    vim +PlugInstall! +qall 2>/dev/null
fi
