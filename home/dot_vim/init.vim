" Neovim config: source the shared vim config from ~/.vimrc
" All shared settings live in ~/.vimrc (managed via chezmoi).
" Add neovim-specific settings below as you migrate from vim.
source $HOME/.vimrc
